:star:

$W_1$是不变子空间,则$\mathscr A$在扩基下的矩阵是分块上三角矩阵,其中$A_1$就是$\mathscr A_{W_1}$的在$W_1$基下的矩阵,且$\varphi(\lambda)=\varphi_1(\lambda)h(\lambda)$.

$V$有不变子空间直和分解,则$\mathscr A$在并基下的矩阵就是分块对角矩阵,其中$A_i$是对应的矩阵,且$\varphi(\lambda)=\prod\varphi_i(\lambda)$.

证明:

扩基不管自然是$A_1$了.对$A_i$有矩阵的特征多项式.



Q&A:补子空间存在,即天然有$V=W\oplus W'$ 但是$W'$不一定是不变子空间.

---

*不变子空间直和分解来实现化简*

---

获取不变子空间的方法:循环子空间$\mathscr L(\alpha,\mathscr A\alpha,\cdots)$ 

---

C-H定理: $\varphi(\mathscr A)=\mathscr O$

拓展:A可逆则存在$f(\lambda)$使得$A^{-1}=f(A)$

---

*前面是从直和分解得到的特征多项式分解,下面从特征多项式分解得到直和分解.*

---

:star:

引理: 互素则乘积有核空间直和分解,也是不变子空间直和分解

$\varphi(\lambda)=\prod(\lambda-\lambda_i)^{n_i}$ 则$V$有根子空间直和分解 根子空间$W_{\lambda_i}=ker(\mathscr A-\lambda_i\mathscr E)^{n_i}$

证明:
$$
\varphi(\mathscr A)=\prod \varphi_i(\mathscr A)\quad \varphi_i(\mathscr A)=(\mathscr A-\lambda_i\mathscr E)^{n_i}
$$
由引理,有$ker\varphi(\mathscr A)$核空间直和分解.

由C-H定理,有$ker\varphi(\mathscr A)=ker\mathscr O=V$.

---

升链是核空间扩大的过程(因为作用一次为零自然作用两次为零,反之不然),且根子空间最大

核空间升链存在化零指数

证明:略

---

最小多项式整除任意化零多项式

证明:

次数小,可作带余除法,代入A,得到余式的A多项式为O.

因为规定$\mu$是最小化零,余式次数小于$\mu$,所以只能余式本身为0,与是否代入A无关.

---

特征多项式与最小多项式同根

证明:

因为整除,所以$\mu$的根也是$\varphi$的根.

因为是特征值,所以A作用与$\lambda$作用效果一样,$\mu(A)\xi=\mu(\lambda)\xi$,因为化零,所以$\mu(A)\alpha=O\alpha=O$,即$\mu(\lambda)\xi=0$,即是$\mu$的根

---

A的最小多项式是分块最小多项式的最小公倍式

证明:因为都整除

---

可对角=最小多项式无重根

证明:

可对角=特征子空间直和,而特征子空间就是ker($\mathscr A-\lambda_i\mathscr E$),已是化零,即最小,无重根

反之由互素核空间分解引理结合化零,得到特征子空间直和分解

---

根子空间维数=代数重数

---

因为是幂零变换,其循环子空间是天然的Jordan块(下次对角为1)

W有这样一组基 该基是一系列$\alpha$生成的幂零变换循环子空间的并基

证明就没想让我懂,略

从而W可以Jordan分解,每个块都是Jordan块,从而这样的矩阵是Jordan形矩阵,后续加回$\lambda_i$就大功告成了

---

此时我们关心每个Jordan块的尺寸,以及Jordan块的总个数



